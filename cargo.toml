[package]
name = "reflex-runtime"
version = "0.1.0"
edition = "2021"
authors = ["cmiller-wq"]

[dependencies]
# Utilizing standard traits for absolute zero-trust validation layers
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
sha2 = "0.10"
